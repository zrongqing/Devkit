# Devkit 协作规则

## 仓库边界

- `src/web`、`src/server`、`src/client` 为三个独立交付物；不直接共享跨语言源码。
- 服务端 OpenAPI 和版本化 HTTP 契约是三端交互的唯一事实来源。
- 技术栈尚未定型的事项记录在各端 `TECH_STACK.md`，不要将个人偏好当作既定标准。

## 变更流程

- 改接口时，同一个变更必须同步更新服务端、Web 调用模板、Desktop 调用模板及契约测试。
- 保持最小分层依赖，禁止 UI 直接访问数据库或绕过应用服务。
- 新功能至少提供成功、网络/服务器失败和配置缺失三类验证。
- 不提交许可证、密钥、真实连接串、构建产物或本机配置。

## 验证

- .NET：执行受影响解决方案的 `dotnet build` 与 `dotnet test`。
- Web：执行 `npm run build`；新增依赖需锁定并说明用途。
