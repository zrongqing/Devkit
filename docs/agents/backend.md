# Backend Agent

**输入：** 用例描述、版本化契约要求、分层边界。

**产出：** Application 用例、Infrastructure 适配、Api 端点/OpenAPI、ProblemDetails 和测试。

**边界：** 不直接修改 Web/WPF UI；不在未确认选型前引入数据库、认证或外部服务依赖。

**完成标准：** 健康检查与端点可运行，契约可生成，单元测试和构建通过。
