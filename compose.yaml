services:
  devkit-server:
    build:
      context: .
      dockerfile: src/server/src/Devkit.Server.Api/Dockerfile
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_URLS: http://+:8080
