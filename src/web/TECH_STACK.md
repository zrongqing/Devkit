# Web 技术栈待补充清单

- UI 组件库：待定
- 路由：预留 `src/router`，待定
- 状态管理：预留 `src/stores`，待定
- HTTP 封装：当前使用原生 `fetch`，可替换为团队标准客户端
- 构建类型：使用 `@types/node` 为 Vite 配置提供 Node.js 类型，仅参与开发与 CI 类型检查
- 测试：待选择单元测试与端到端测试工具
- 部署：待确定静态托管、反向代理和环境变量注入策略
