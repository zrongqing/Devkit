<UserControl x:Class="Ssamc.Views.WebappUpdateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Ssamc.Views"
             mc:Ignorable="d"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:behaviors="clr-namespace:Devkit.Core.UI.Behaviors;assembly=Devkit.Core.UI"
             xmlns:viewModels="clr-namespace:Ssamc.ViewModels"
             xmlns:coreVM ="clr-namespace:Devkit.Core.UI.Models;assembly=Devkit.Core.UI"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             d:Background="White"
             d:DataContext="{d:DesignInstance viewModels:WebappUpdateViewModel}"
             d:DesignHeight="800"
             d:DesignWidth="1200"
             prism:ViewModelLocator.AutoWireViewModel="True"
             >
  <UserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/SkinDefault.xaml" />
        <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/Theme.xaml" />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </UserControl.Resources>

  <Grid>
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="auto"></RowDefinition>
        <RowDefinition Height="*"></RowDefinition>
      </Grid.RowDefinitions>

      <Grid Grid.Row="0">
        <StackPanel Orientation="Horizontal">
          <Button Content="git pull" 
                  Visibility="Collapsed"/>
          <Button Command="{Binding SearchFilesCommand}"
                  CommandParameter="{Binding Text,ElementName=WebappPath}"
                  Content="读取文件夹"
                  Style="{StaticResource ButtonPrimary}" />
          <Button Command="{Binding UpdateFilesCommand}"
                  CommandParameter="215.22"
                  Margin="10,0,0,0"
                  Content="215.22"
                  Style="{StaticResource ButtonPrimary}" />
          <Button Command="{Binding UpdateFilesCommand}"
                  CommandParameter="20.53"
                  Margin="10,0,0,0"
                  Content="20.53"
                  Style="{StaticResource ButtonPrimary}" />
          <Button Command="{Binding UpdateFilesCommand}"
                  CommandParameter="production"
                  Margin="10,0,0,0"
                  Content="production"
                  Style="{StaticResource ButtonPrimary}" />
        </StackPanel>
      </Grid>

      <Grid Grid.Row="1">
        <Grid.RowDefinitions>
          <RowDefinition Height="auto"/>
          <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical">

          <TextBox Margin="0,10,0,0"
                   x:Name="WebappPath"
                   hc:InfoElement.Necessary="True"
                   hc:InfoElement.Title="webapp path:"
                   IsReadOnly="False"
                   Style="{StaticResource TextBoxExtend}"
                   Text="{Binding WebappPath,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
          </TextBox>

        </StackPanel>

        <Grid Grid.Row="1">
          <!-- 树视图区域 -->
          <Border Grid.Row="1"
                  Background="White"
                  BorderBrush="{DynamicResource BorderBrush}"
                  BorderThickness="1"
                  CornerRadius="4">

            <!-- 使用hc:TreeView -->
            <TreeView ItemsSource="{Binding TreeNodes}"
                         VirtualizingPanel.IsVirtualizing="True"
                         VirtualizingPanel.VirtualizationMode="Recycling"
                         Background="Transparent"
                         BorderThickness="0">

              <TreeView.ItemTemplate>
                <HierarchicalDataTemplate DataType="{x:Type coreVM:TreeNodeModel}"
                                          ItemsSource="{Binding Children}">

                  <!-- 使用复选框和文本 -->
                  <StackPanel Orientation="Horizontal"
                              Margin="2"
                              VerticalAlignment="Center">

                    <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                              VerticalAlignment="Center"
                              Margin="0,0,5,0" />

                    <!-- 使用hc:Shield或文本显示节点名称 -->
                    <TextBlock Text="{Binding Name}"
                               VerticalAlignment="Center"
                               FontSize="14" />
                  </StackPanel>
                </HierarchicalDataTemplate>
              </TreeView.ItemTemplate>

              <!-- 节点样式：添加悬停效果和展开折叠图标 -->
              <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem"
                       BasedOn="{StaticResource TreeViewItemBaseStyle}">
                  <Setter Property="IsExpanded"
                          Value="{Binding IsExpanded, Mode=TwoWay}" />
                  <Setter Property="Padding"
                          Value="5,3" />
                  <Setter Property="HorizontalContentAlignment"
                          Value="Stretch" />
                  <Setter Property="VerticalContentAlignment"
                          Value="Center" />
                </Style>
              </TreeView.ItemContainerStyle>
            </TreeView>
          </Border>
        </Grid>
        
      </Grid>
      
    </Grid>
  </Grid>
  
</UserControl>
