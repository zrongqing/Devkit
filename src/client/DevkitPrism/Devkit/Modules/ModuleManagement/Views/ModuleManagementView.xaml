<UserControl x:Class="Devkit.Modules.ModuleManagement.Views.ModuleManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="720"
             MinHeight="420">
  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <DockPanel LastChildFill="True">
      <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
        <Button Margin="8,0,0,0"
                Padding="16,8"
                Command="{Binding RefreshCommand}"
                Content="刷新目录" />
        <Button Margin="8,0,0,0"
                Padding="16,8"
                Command="{Binding AddModuleCommand}"
                Content="选择 DLL 并加载" />
      </StackPanel>
      <StackPanel>
        <TextBlock FontSize="24" FontWeight="SemiBold" Text="模块管理" />
        <TextBlock Margin="0,6,16,0"
                   Foreground="#64748B"
                   Text="加载和卸载独立 Prism 模块；卸载不会删除磁盘文件。" />
      </StackPanel>
    </DockPanel>

    <Border Grid.Row="1"
            Margin="0,18,0,12"
            Padding="12,8"
            Background="#F1F5F9"
            CornerRadius="4">
      <TextBlock Text="{Binding ModuleDirectory, StringFormat=固定模块目录：{0}}"
                 TextTrimming="CharacterEllipsis"
                 ToolTip="{Binding ModuleDirectory}" />
    </Border>

    <DataGrid Grid.Row="2"
              AutoGenerateColumns="False"
              CanUserAddRows="False"
              GridLinesVisibility="Horizontal"
              IsReadOnly="True"
              ItemsSource="{Binding Modules}"
              RowHeaderWidth="0">
      <DataGrid.Columns>
        <DataGridTextColumn Width="210" Header="模块" Binding="{Binding ModuleId}" />
        <DataGridTextColumn Width="100" Header="版本" Binding="{Binding Version}" />
        <DataGridTextColumn Width="130" Header="状态" Binding="{Binding Status}" />
        <DataGridTextColumn Width="*" MinWidth="220" Header="程序集路径" Binding="{Binding AssemblyPath}" />
        <DataGridTemplateColumn Width="170" Header="操作">
          <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
              <StackPanel Orientation="Horizontal">
                <Button Padding="12,5"
                        Command="{Binding DataContext.LoadModuleCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                        CommandParameter="{Binding}"
                        Content="加载">
                  <Button.Style>
                    <Style TargetType="Button">
                      <Setter Property="Visibility" Value="Visible" />
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLoaded}" Value="True">
                          <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsAvailable}" Value="False">
                          <Setter Property="IsEnabled" Value="False" />
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </Button.Style>
                </Button>
                <Button Margin="8,0,0,0"
                        Padding="12,5"
                        Command="{Binding DataContext.UnloadModuleCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                        CommandParameter="{Binding}"
                        Content="卸载">
                  <Button.Style>
                    <Style TargetType="Button">
                      <Setter Property="Visibility" Value="Collapsed" />
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLoaded}" Value="True">
                          <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </Button.Style>
                </Button>
              </StackPanel>
            </DataTemplate>
          </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>
        <DataGridTemplateColumn Width="240" Header="错误">
          <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
              <TextBlock Foreground="#B91C1C"
                         Text="{Binding ErrorMessage}"
                         TextTrimming="CharacterEllipsis"
                         ToolTip="{Binding ErrorMessage}" />
            </DataTemplate>
          </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>
      </DataGrid.Columns>
    </DataGrid>
  </Grid>
</UserControl>
